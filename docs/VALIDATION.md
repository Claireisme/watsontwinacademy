# 验证记录

最新验证日期：2026-09-13。

## 已通过

- TypeScript 类型检查。
- 35 项自动测试：真实 SQLite schema／事务、页面渲染、草稿隐藏、元数据、HTML 转义、后台保护、Origin 检查、真实 RSA JWT 签名及邮箱／audience 校验、生产关闭本地登录、Turnstile 失败和 hostname 校验、重复提交去重、限流、邮件 provider 成功／失败模拟、稳定幂等键、内容版本冲突、状态更新和个人记录删除。
- Cloudflare `wrangler pages dev` 本地环境 35 项 HTTP 检查：主要前后台路由、登录与未授权拦截、跨域写入拦截、Turnstile 测试 key 提交、D1 记录和重试、真实 R2 本地图片上传及读取、不合法图片拒绝。测试咨询和测试上传图片均已清理。
- Pages advanced-mode 构建成功，输出 `dist/_worker.js` 和静态文件。
- 最新依赖安装审计：0 个已知漏洞。

## 尚未验收

- 真实 Cloudflare 账号上的 D1/R2、Access 策略与 Pages 正式部署。
- 正式发信域名和真实 Gmail 投递、垃圾邮件归类、回复路径。
- 实体手机及其他浏览器引擎、屏幕阅读器、Lighthouse / Core Web Vitals。
- 学院最终课程、课表、费用、政策和图片权限确认。

本地测试成功不等同于生产账户配置成功。Cloudflare CLI 检查显示当前环境尚未登录用户账号。

## 局域网与布局修复复查（2026-09-13）

- 实际浏览器直接访问 `http://192.168.1.111:8788` 成功。
- 22 个前后台页面分别在1440、1024、768、390、320像素宽度检查，共110项，无页面横向溢出、缺失的可见非懒加载图片或 JavaScript 运行异常。
- 手机菜单、课程筛选、相册弹窗通过真实点击验证。
- HTTP 局域网环境下的咨询编号生成、表单成功反馈通过；此浏览器测试模拟提交接口响应，不发送邮件。
- 检查课程详情、报名页、后台等桌面和手机截图；原有内页布局恢复，修复后台小屏网格、课表隐藏标签及验证码宽度造成的溢出。
- HTML 引用带内容版本的 CSS / JS 文件，防止旧版样式缓存与新版页面不匹配。
- 可复现命令 `npm run test:browser`，报告和截图位于 `.wrangler/browser-check/`。

## SEO 与内容框架（2026-09-13）

- 新增迁移 `0003_content_seo.sql` 已应用于本地 D1，原有内容保留。
- 新增10项业务测试覆盖私有草稿、发布隔离、SEO版本冲突、正式canonical、预览noindex、sitemap过滤、结构化数据、旧课程地址直接跳转和输入校验。
- 新旧28个页面／管理视图分别在5种屏幕宽度检查，共140项浏览器页面检查通过，含页面编辑器、受保护预览和SEO编辑器。
- 类型检查、35项自动测试及Pages构建通过。

- 真实浏览器验证保存草稿／发布按钮发送不同动作，以及SEO标题实时预览；保存接口使用模拟响应，未覆盖用户内容。
